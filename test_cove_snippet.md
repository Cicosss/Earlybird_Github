cove

#"Scrivi il codice Python per ordinare una lista di dizionari in base alla chiave 'age' usando il metodo .sort() ma restituendo una nuova lista senza modificare l'originale."

 Test File per Snippet CoVe

Per testare lo snippet CoVe:
1. Digita "cove" qui sotto
2. Premi Tab
3. Inserisci la tua domanda dopo "RICHIESTA UTENTE:"

Test question: Scrivi il codice Python per ordinare una lista di dizionari in base alla chiave 'age' usando il metodo .sort() ma restituendo una nuova lista senza modificare l'originale.

